<!-- Since title is a common thing in many pages, we will create a partial and will pass the "TITLE" to it to be rendered according to the page we open -->
<%= render '/shared/page_title', title: "Edit Recipe" %>

<%= render 'form' %>